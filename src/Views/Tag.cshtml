@{
    Layout = "Layout";
    ViewData["Title"] = $"Articles tagged \"{ViewData["Tag"]}\"";
}
@model IEnumerable<Blog.Article>

<h1>@Html.Raw(ViewData["Title"])</h1>

@foreach (var article in Model)
{
    @await Html.PartialAsync("ArticleSummary", article)
}
